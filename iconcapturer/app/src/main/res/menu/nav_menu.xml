<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <group android:checkableBehavior="single">
        <item android:id="@+id/author"
            android:checkable="false"
            android:title="@null"
            app:actionLayout="@layout/menu_author_view"
            />
        <item android:id="@+id/email"
            android:checkable="false"
            android:title="@null"
            app:actionLayout="@layout/menu_email_view"
            />
        <item android:id="@+id/version"
            android:checkable="false"
            android:title="@null"
            app:actionLayout="@layout/menu_version_view"
            />
        <item android:id="@+id/menu_log"
            android:checkable="false"
            android:title="@null"
            app:actionLayout="@layout/menu_log_view"
            />
        <item android:id="@+id/menu_setting"
            android:checkable="false"
            android:title="@null"
            app:actionLayout="@layout/menu_setting_view"
            />
    </group>
</menu>